gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Firebase config for the Play flavor (FCM wake-up push). Per-developer; not
# committed. Place it at app/src/play/google-services.json — the Google Services
# plugin only reads it for Play-flavor builds, so the FOSS build never needs it.
**/google-services.json
